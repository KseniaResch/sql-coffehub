CREATE TABLE worker (

    id INTEGER PRIMARY KEY,
    first_name TEXT NOT NULL,
    second_name TEXT NOT NULL,
    phone_number TEXT NOT NULL,
    email TEXT NOT NULL,
    salary NUMERIC NOT NULL,
     working_hours NUMERIC NULL

);
